name: "First CI/CD with GitHub"

on:
  push:
    tags:
    - 'v*'    

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v2
        - name: Configure AWS credentials
          uses: aws-actions/configure-aws-credentials@v2
          with:
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws-region: us-east-1
            
        - name: Build Docker image
          env:
            IMAGE_TAG: latest
          run: |
            DOCKER_TAG=$(echo "${GITHUB_REF}" | cut -d'/' -f 3)
            docker build -t gic-dx-its-mgt-front:$IMAGE_TAG .
            docker tag gic-dx-its-mgt-front:$IMAGE_TAG 339712794622.dkr.ecr.us-east-1.amazonaws.com/test-cicd:${DOCKER_TAG}
            docker tag gic-dx-its-mgt-front:$IMAGE_TAG 339712794622.dkr.ecr.us-east-1.amazonaws.com/test-cicd:$IMAGE_TAG

        - name: Authenticate to Amazon ECR
          run: |
            aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 339712794622.dkr.ecr.us-east-1.amazonaws.com

        - name: Push Docker image to Amazon ECR
          run: |
            DOCKER_TAG=$(echo "${GITHUB_REF}" | cut -d'/' -f 3)
            docker push 339712794622.dkr.ecr.us-east-1.amazonaws.com/test-cicd:${DOCKER_TAG}
            docker push 339712794622.dkr.ecr.us-east-1.amazonaws.com/test-cicd:latest